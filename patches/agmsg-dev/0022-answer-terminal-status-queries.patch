diff --git a/app/src/TerminalPane.tsx b/app/src/TerminalPane.tsx
--- a/app/src/TerminalPane.tsx
+++ b/app/src/TerminalPane.tsx
@@ -67,2 +67,38 @@
     const unlisteners: Array<() => void> = [];
+    const decoder = new TextDecoder();
+    let queryBuffer = "";
+    const terminalQueries: Array<{ sequence: string; reply: string }> = [
+      { sequence: "\x1b[6n", reply: "\x1b[1;1R" },
+      { sequence: "\x1b[5n", reply: "\x1b[0n" },
+      { sequence: "\x1b[c", reply: "\x1b[?1;2c" },
+      // Codex asks for the current Kitty keyboard protocol flags.
+      { sequence: "\x1b[?u", reply: "\x1b[?0u" },
+      // xterm.js does not answer OSC color queries through the PTY.
+      { sequence: "\x1b]10;?\x1b\\", reply: "\x1b]10;rgb:ffff/ffff/ffff\x1b\\" },
+      { sequence: "\x1b]11;?\x1b\\", reply: "\x1b]11;rgb:0000/0000/0000\x1b\\" },
+      { sequence: "\x1b]10;?\x07", reply: "\x1b]10;rgb:ffff/ffff/ffff\x07" },
+      { sequence: "\x1b]11;?\x07", reply: "\x1b]11;rgb:0000/0000/0000\x07" },
+    ];
+    const respondToTerminalQueries = (bytes: Uint8Array) => {
+      // A real terminal answers device-status queries generated by TUIs. The
+      // PTY chunks can split an escape sequence, so retain only the short
+      // suffix needed to complete the next query and consume matched queries.
+      queryBuffer += decoder.decode(bytes, { stream: true });
+      const replies: string[] = [];
+      let cursor = 0;
+      while (cursor < queryBuffer.length) {
+        let match: { index: number; sequence: string; reply: string } | null = null;
+        for (const query of terminalQueries) {
+          const index = queryBuffer.indexOf(query.sequence, cursor);
+          if (index !== -1 && (!match || index < match.index)) {
+            match = { index, ...query };
+          }
+        }
+        if (!match) break;
+        replies.push(match.reply);
+        cursor = match.index + match.sequence.length;
+      }
+      queryBuffer = queryBuffer.slice(Math.max(cursor, queryBuffer.length - 12));
+      if (replies.length) void invoke("pty_write", { id, data: replies.join("") });
+    };
     (async () => {
@@ -71,3 +85,7 @@
         await listen<{ id: string; b64: string }>("pty-output", (e) => {
-          if (e.payload.id === id) term.write(b64ToBytes(e.payload.b64));
+          if (e.payload.id === id) {
+            const bytes = b64ToBytes(e.payload.b64);
+            term.write(bytes);
+            respondToTerminalQueries(bytes);
+          }
         }),
