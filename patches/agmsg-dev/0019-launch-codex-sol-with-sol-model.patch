diff --git a/app/src/App.tsx b/app/src/App.tsx
--- a/app/src/App.tsx
+++ b/app/src/App.tsx
@@ -523,5 +523,9 @@ export default function App() {
       const cmdPrefix = type ? (freshCmdPrefixFor.get(type) ?? "/") : "/";
       const options = type ? (freshOptionsFor.get(type) ?? []) : [];
+      const memberOptions =
+        type === "codex" && m.name === "codex-sol"
+          ? ["-m", "gpt-5.6-sol", ...options]
+          : options;
       // Treat app-spawned panes as app-delivered by default. Probing
       // delivery.sh status can block on per-tool hooks files (notably Codex's
       // .codex/hooks.json from a GUI sandbox), and pane startup must never
@@ -550,7 +554,7 @@ export default function App() {
             label: m.name,
             team,
             cmd: launchCmd ?? cli,
-            args: type === "codex" ? [...options] : [...options, actasPrompt],
+            args: type === "codex" ? [...memberOptions] : [...memberOptions, actasPrompt],
             cwd: m.project || undefined,
             cmdPrefix,
             native: monitors,
