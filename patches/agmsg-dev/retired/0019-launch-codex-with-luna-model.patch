diff --git a/app/src/App.tsx b/app/src/App.tsx
index 76aa0f3..b8cd76c 100644
--- a/app/src/App.tsx
+++ b/app/src/App.tsx
@@ -562,5 +562,9 @@ export default function App() {
       const cmdPrefix = type ? (freshCmdPrefixFor.get(type) ?? "/") : "/";
       const options = type ? (freshOptionsFor.get(type) ?? []) : [];
+      const memberOptions =
+        type === "codex" && m.name === "Codex"
+          ? ["-m", "gpt-5.6-luna", "-c", 'model_reasoning_effort="max"', ...options]
+          : options;
       // `native` = "this (type, project) actually self-delivers agmsg
       // messages" — asked from agmsg's own delivery.sh status (mode derived
       // from the project's real hooks file), NOT a static type.conf flag.
@@ -591,7 +595,7 @@ export default function App() {
             label: m.name,
             team,
             cmd: launchCmd ?? cli,
-            args: [...options, `${cmdPrefix}${cmdName} actas ${m.name}`],
+            args: [...memberOptions, `${cmdPrefix}${cmdName} actas ${m.name}`],
             cwd: m.project || undefined,
             cmdPrefix,
             native: monitors,
